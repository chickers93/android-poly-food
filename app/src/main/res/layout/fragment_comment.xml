<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="Fragment.Comment_Fragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingVertical="10dp">

        <RatingBar
            android:id="@+id/ratingBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="15dp"
            android:numStars="5"
            android:stepSize="1"
            android:theme="@style/RatingBar" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/ed_comment"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_marginHorizontal="5dp"
                android:layout_weight="3.5"
                android:background="@drawable/border_add_text5"
                android:fontFamily="@font/icielplanton"
                android:gravity="center_vertical"
                android:hint="Đánh giá của bạn về cửa hàng"
                android:inputType="textMultiLine"
                android:lineSpacingExtra="5dp"
                android:paddingLeft="10dp"
                android:textSize="16sp" />

            <Button
                android:id="@+id/btn_comment"
                android:layout_width="0dp"
                android:layout_height="45dp"
                android:layout_marginRight="5dp"
                android:layout_weight="1"
                android:background="@drawable/custom_btn"
                android:fontFamily="@font/icielplanton"
                android:gravity="center"
                android:text="Đăng"
                android:textColor="@color/white"
                android:textStyle="bold" />

        </LinearLayout>


    </LinearLayout>

    <LinearLayout
        android:id="@+id/layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_menu"
        android:orientation="vertical"
        android:weightSum="10">

        <ListView
            android:id="@+id/lv_comment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginHorizontal="5dp"
            android:layout_weight="10"
            android:background="@drawable/border_add_text6"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:padding="5dp">

        </ListView>

    </LinearLayout>

</LinearLayout>
